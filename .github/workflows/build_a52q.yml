name: Build Kernel for A52 (a52q)

on:
  workflow_dispatch:

jobs:
  build:
    name: Build Kernel
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Kernel
        run: |
          echo "Bắt đầu build kernel cho Galaxy A52..."
          echo "Profile: a52q_oneui.env"
          echo "Chờ script build chạy..."
